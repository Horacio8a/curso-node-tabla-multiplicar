# Nota:
Este es mi primer programa en Node

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     esta es la base de la tabla de multiplicar
                                              [cadena de caracteres] [requerido]
  -l, --listar   Muestra la tabla en la consola      [booleano] [defecto: false]
  -h, --hasta    Este es el numero hasta donde quieres la tabla
                                              [número] [requerido] [defecto: 10]
```